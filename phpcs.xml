<?xml version="1.0"?>
<ruleset name="FacturaScripts Plugin">
    <description>Estándar de código para plugins de FacturaScripts basado en PSR-12</description>

    <!-- Archivos a comprobar -->
    <file>Controller</file>
    <file>Extension</file>
    <file>Lib</file>
    <file>Model</file>
    <file>Worker</file>
    <file>Test</file>
    <file>Init.php</file>

    <!-- Patrones excluidos -->
    <exclude-pattern>Assets/</exclude-pattern>
    <exclude-pattern>docker/</exclude-pattern>
    <exclude-pattern>vendor/</exclude-pattern>
    <exclude-pattern>View/</exclude-pattern>
    <exclude-pattern>XMLView/</exclude-pattern>
    <exclude-pattern>Test/bootstrap.php</exclude-pattern>
    <exclude-pattern>Test/install-plugins.php</exclude-pattern>

    <!-- Estándar PSR-12 -->
    <rule ref="PSR12"/>

    <!-- Reglas adicionales -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="0"/>
        </properties>
    </rule>
    <rule ref="Generic.PHP.ForbiddenFunctions">
        <properties>
            <property name="forbiddenFunctions" type="array">
                <element key="sizeof" value="count"/>
                <element key="delete" value="unset"/>
                <element key="print" value="echo"/>
                <element key="create_function" value="null"/>
            </property>
        </properties>
    </rule>

    <!-- Progreso y colores -->
    <arg value="ps"/>
    <arg name="colors"/>

    <!-- Procesamiento en paralelo -->
    <arg name="parallel" value="75"/>
</ruleset>
